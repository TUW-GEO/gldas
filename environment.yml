name: gldas
channels:
  - conda-forge
  - defaults
dependencies:
  - pandas
  - numpy
  - pygrib
  - netCDF4
  - pyresample
  # pip dependencies are defined in setup.cfg
  - pip:
      - setuptools-scm